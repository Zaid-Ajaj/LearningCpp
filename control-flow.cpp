#include <iostream>
#include <string>
#include <vector>
#include <fstream>

using namespace std;

int main() 
{
    bool isFinished = true;
    if (isFinished) {
        cout << "It is finished" << endl;
    } else {
        cout << "It is not finished" << endl;
    }
    return 0;
}